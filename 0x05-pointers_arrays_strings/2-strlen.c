#include "main.h"

/**
 * _strlen - returns the lenght of a string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int longi = o;

	while (*s != '\0')
	{
		longi++;
		s++;
	}
	return (longi);
}
